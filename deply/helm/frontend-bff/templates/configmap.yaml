apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend-bff.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "frontend-bff.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
